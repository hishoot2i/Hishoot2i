<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fitsSystemWindows="true"
        tools:context=".ui.activity.MainActivity">

    <include layout="@layout/include_appbarlayout_toolbar"/>

    <FrameLayout
            android:id="@+id/flContent"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_gravity="center"
            android:paddingTop="?attr/actionBarSize"/>

    <android.support.design.widget.FloatingActionButton
            android:id="@+id/fab"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="@dimen/fab_margin_vertical"
            android:layout_marginStart="@dimen/fab_margin_horizontal"
            android:layout_marginLeft="@dimen/fab_margin_horizontal"
            android:src="@drawable/ic_notif"
            app:fabSize="normal"
            app:layout_anchor="@+id/flContent"
            app:layout_anchorGravity="bottom|left|start"/>
</android.support.design.widget.CoordinatorLayout>